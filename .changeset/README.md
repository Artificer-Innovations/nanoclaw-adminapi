# Changesets

1. Open PRs against `develop` with a changeset (`pnpm changeset`)
2. Run `pnpm run version` on `develop` before merging to `main`
3. Merge to `main` triggers release publish with provenance
