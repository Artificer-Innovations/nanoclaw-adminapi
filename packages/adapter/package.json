{
  "name": "@nanoclaw-adminapi/adapter",
  "version": "0.1.0",
  "private": true,
  "description": "NanoClaw admin API adapter source (installed into host forks at runtime)",
  "type": "module",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test:unit": "vitest run",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@types/node": "^22.15.30",
    "@vitest/coverage-v8": "^4.1.9",
    "typescript": "^5.8.3",
    "vitest": "^4.1.9"
  }
}
